export function registerBMadCommand(program) {
  // Minimal stub for the BMAD CLI registration. The real implementation
  // can provide interactive workflows and commands; this stub keeps the
  // CLI import happy during local builds.
  program.command && program; // noop to avoid lint complaint
}
