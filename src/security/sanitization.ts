export function sanitizeUserInput(text: string): string {
  // Minimal no-op sanitization placeholder. Implement real sanitization logic
  // in a follow-up change that matches project security requirements.
  return text;
}
